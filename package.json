{
  "name": "react-intl-validator",
  "version": "0.1.0",
  "description": "A Node CLI to validate and extract i18n keys in React projects using react-intl-universal. Checks for missing/inconsistent translations.",
  "main": "index.js",
  "bin": {
    "intl-validate": "./bin/i18n-check.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint .",
    "start": "node bin/i18n-check.js"
  },
  "keywords": [
    "react",
    "i18n",
    "validation",
    "intl-universal",
    "cli"
  ],
  "author": "songyu",
  "license": "MIT",
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "glob": "^10.4.5"
  },
  "devDependencies": {
    "eslint": "^9.6.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
